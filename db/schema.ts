import { sqliteTable, text, integer, index, primaryKey } from 'drizzle-orm/sqlite-core';
export const entries = sqliteTable('entries', { id: text('id').primaryKey(), ownerId: text('owner_id').notNull(), kind: text('kind').notNull(), title: text('title').notNull(), description: text('description').notNull(), category: text('category').notNull(), author: text('author').notNull(), body: text('body').notNull(), protocol: text('protocol').notNull(), endpoint: text('endpoint').notNull(), docsUrl: text('docs_url').notNull(), auth: text('auth').notNull(), permissions: text('permissions').notNull(), privacyUrl: text('privacy_url').notNull(), termsUrl: text('terms_url').notNull(), supportUrl: text('support_url').notNull(), status: text('status').notNull(), createdAt: text('created_at').notNull() }, t => [index('entries_status_created').on(t.status, t.createdAt), index('entries_owner').on(t.ownerId)]);
export const interactions = sqliteTable('interactions', { ownerId: text('owner_id').notNull(), entryId: text('entry_id').notNull(), type: text('type').notNull() }, t => [primaryKey({ columns: [t.ownerId, t.entryId, t.type] }), index('interactions_entry_type').on(t.entryId, t.type)]);
export const comments = sqliteTable('comments', { id: text('id').primaryKey(), ownerId: text('owner_id').notNull(), entryId: text('entry_id').notNull(), author: text('author').notNull(), body: text('body').notNull(), createdAt: text('created_at').notNull() }, t => [index('comments_entry_created').on(t.entryId, t.createdAt)]);
export const rateLimits = sqliteTable('rate_limits', { key: text('key').primaryKey(), count: integer('count').notNull(), expiresAt: integer('expires_at').notNull() });
